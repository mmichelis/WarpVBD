# WarpVBD

Implementing VBD in NVIDIA Warp.

## Installation

Just PBRT-v3 needs to be built. Run `./install.sh` for this build with CMake.


## Notes

The compilation of the VBD solver kernel takes about 20s, so please be patient the first time the warp kernel is compiled.